<nav class="navbar navbar-expand-lg navbar-light bg-transparent">
  <a class="navbar-brand ms-4" href="#"><img id="logo" src="./images/logo.png" alt=""></a>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavAltMarkup"
    aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>
  <div class="collapse navbar-collapse justify-content-between" id="navbarNavAltMarkup">
    <div class="navbar-nav navbar-items ms-4">
      <a class="nav-item nav-link" href="/">Home</a>
      <a class="nav-item nav-link text-danger" href="#menu">Menu</a>
      <a class="nav-item nav-link text-danger" href="order">Order Now</a>
      <a id="login" class="nav-item nav-link text-danger" href="login">Login</a>
    </div>
    <div>
      <!-- <img id="userImage" src="" alt=""> -->
      <h6 id="user"class="text-light me-5">Welcome <a id="username" class="nav-item nav-link d-inline" href="/profile"></a></h6>
    </div>
  </div>
</nav>

<script>

  $("#username").hover(()=> {
      // over
      $("#username").addClass("link-light");
      $("#username").removeClass("nav-link")
      
    }, ()=> {
      // out
      $("#username").addClass("nav-link")

      $("#username").removeClass("link-light")
    }
  );
  const user = JSON.parse(localStorage.getItem('userData'));
  console.log(user);
  if (user !== null) {
    // User is signed in, see docs for a list of available properties
    // https://firebase.google.com/docs/reference/js/firebase.User
    // const uid = user.uid;
    $("#username").html(user.displayName);
    $("#login").hide();
    // ...
  } else {
    // User is signed out
    // ...
    $("#user").hide();
  }

</script>